import { AxiosResponse } from 'axios';
import GithubClient from './githubClient';
declare class GitHubRelease {
    token: string;
    username: string;
    repository: string;
    releaseNotes: string;
    commitHash?: string;
    git: GithubClient;
    constructor(token: string, username: string, repository: string, releaseNotes: string, commitHash?: string);
    publishAssets(srcLocation: string, destUrl: string): Promise<AxiosResponse<any, any>>[];
    release(): Promise<void>;
}
export { GitHubRelease };
